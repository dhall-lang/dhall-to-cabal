name: Name
version: 1
cabal-version: 2.0
build-type: Simple
license: UnspecifiedLicense

library
    
    if impl(ghc >=8.2)
        build-depends:
            B -any
    else
    
    if impl(ghc >=8.4)
        build-depends:
            C -any
    else
    build-depends:
        A -any


