cabal-version: 2.2
name: Name
version: 1

library
    default-language: Haskell2010
    build-depends:
        A -any
    
    if impl(ghc >=8.2)
        build-depends:
            B -any
    else
    
    if impl(ghc >=8.4)
        build-depends:
            C -any
    else
